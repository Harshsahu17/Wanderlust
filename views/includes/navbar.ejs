<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid flex-wrap gap-2">

    <!-- Brand -->
    <a class="navbar-brand" href="/listings">
      <i class="fa-regular fa-compass"></i>
      <b class="brand">Wanderlust</b>
    </a>

    <!-- Search bar: always visible, outside collapse, with min-width:0 fix -->
    <form class="search-form order-2 order-md-0 flex-md-grow-1 mx-md-3" role="search" action="/listings" method="GET">
      <input
        class="form-control search-inp"
        type="search"
        placeholder="Search destination..."
        name="search"
        aria-label="Search"
        style="min-width:0;"
      />
      <button class="btn search-btn" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>
        <span class="search-text"> Search</span>
      </button>
    </form>

    <!-- Toggler: order-3 on mobile so it sits after search, always at far right -->
    <button
      class="navbar-toggler order-3 order-md-0 ms-auto"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible links -->
    <div class="collapse navbar-collapse order-4 w-100" id="navbarNav">
      <div class="nav-links-group navbar-nav ms-auto text-end">
        <a class="nav-link text-end" href="/listings">Explore</a>
        <a class="nav-link text-end" href="/listings/new">Host</a>
        <% if (!currUser) { %>
          <a class="nav-link text-end" href="/signup"><b>Sign up</b></a>
          <a class="nav-link text-end" href="/login"><b>Log in</b></a>
        <% } %>
        <% if (currUser) { %>
          <a class="nav-link text-end" href="/logout"><b>Log out</b></a>
        <% } %>
      </div>
    </div>

  </div>
</nav>
